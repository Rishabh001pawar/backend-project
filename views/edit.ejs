<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1 class="text-bold  ">Edit Post</h1>

    <form method="post" action="/edit/<%= post._id %>" class="w-full max-w-lg">
        <textarea
          name="content"
          rows="10" cols="30"
          class="resize-none bg-transparent border-2 border-zinc-700 text-white w-full h-32 rounded-md outline-none px-3 py-2 mb-4"
        ><%= post.content %></textarea>

        <button
          type="submit"
          class="bg-blue-500 hover:bg-blue-700 text-white px-4 py-2 rounded-md"
        >
          update post
        </button>
      </form>
</body>
</html>